{
  "name": "heelseek",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Every UNC Chapel Hill event in one place, including the club ones",
  "scripts": {
    "ingest": "node scripts/ingest.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.mjs",
    "refresh": "npm run ingest && npm run build",
    "preview:image": "node scripts/make-preview.mjs"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "playwright": "^1.62.1",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
